name: '[trigger] [all branches] Push, PR'

on: [push, pull_request]

jobs:
  build:

    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1

    - name: Run 'build-all'
      uses: ./.github/workflows/build-all
